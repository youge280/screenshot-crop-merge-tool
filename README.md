# 截圖區域合併工具

這是一個只在瀏覽器內執行的截圖工具，可以從一張截圖中選取多個區域，並合併匯出成一張 PNG。

## 功能

- 從電腦開啟圖片
- 從剪貼簿貼上圖片
- 支援連續加入多張截圖，跨多張圖選取區域後合併成一張圖
- 手動選取多個區域
- 自動辨識紅框標出的區域
- 調整已選區域順序
- 支援上下合併或左右合併
- 固定顯示繁體中文介面

## 隱私

所有圖片處理都在瀏覽器內完成，不會上傳到伺服器。

## GitHub Pages

這是一個靜態網頁專案，可直接用 GitHub Pages 從倉庫根目錄發布。
